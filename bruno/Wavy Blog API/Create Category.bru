meta {
  name: Create Category
  type: http
  seq: 8
}

post {
  url: {{host}}/api/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "New Category"
  }
}