meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{host}}/api/users/register
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser",
    "email": "test@example.com",
    "password": "password123"
  }
}

script:post-response {
  const jsonData = res.getBody();
  bru.setVar("userID", jsonData.id);
}