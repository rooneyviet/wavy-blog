meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{host}}/api/users/login
  body: json
  auth: none
}

body:json {
  {
    "email": "test@example.com",
    "password": "password123"
  }
}

script:post-response {
  const jsonData = res.getBody();
  bru.setVar("access_token", jsonData.token);
}