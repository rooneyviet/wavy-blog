meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{host}}/api/users/{{userID}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "username": "testuser-updated",
    "email": "test-updated@example.com"
  }
}